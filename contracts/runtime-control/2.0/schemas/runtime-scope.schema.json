{
  "$id": "https://agents-anywhere.dev/contracts/runtime-control/2.0/schemas/runtime-scope.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Runtime Control 2.0 scope. runtime is always the provider type; runtimeId selects one immutable instance of that provider.",
  "properties": {
    "runtime": {
      "description": "Stable provider type such as codex, claude, or dsh.",
      "maxLength": 64,
      "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$",
      "type": "string"
    },
    "runtimeId": {
      "description": "Concrete runtime instance identifier. Legacy compatibility instances use runtimeId equal to runtime.",
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$",
      "type": "string"
    }
  },
  "required": [
    "runtime",
    "runtimeId"
  ],
  "title": "RuntimeScope",
  "type": "object",
  "x-direction": "bidirectional",
  "x-runtime-control-version": "2.0"
}
