# DSH Bridge Plugin

This directory is reserved for the DSH Desktop plugin code that exposes the
Agents Anywhere bridge from inside the DSH host process.

The protocol contract lives under `contracts/dsh-bridge/`. Connector-side
runtime integration lives under `connector/connector/runtimes/dsh/`.
