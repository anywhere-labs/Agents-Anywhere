# 已归档的 DSH Bridge 实现

此目录保存从 `dsh-bridge/` 替换下来的 `@agents-anywhere/dsh-bridge` 0.1.0。
替换前 Git 检查点：`e6ef163`。

当前插件位于 [`../../dsh-bridge/`](../../dsh-bridge/)，来源为
`codex/dsh-aa-gateway` 分支提交 `aee9d562` 的 `dsh-aa-gateway/`。
本目录仅供历史查阅，不参与构建或发布。
