# Retired Runtime Control negotiation

These artifacts preserve the previous version negotiation and fallback tests.
They are not part of the active test suite. The single release protocol uses
empty discovery parameters, typed provider descriptors, and explicit instance
identity for every lifecycle request. Current coverage lives in the Connector
contract tests and Server runtime discovery, routing, repository and ingress tests.
