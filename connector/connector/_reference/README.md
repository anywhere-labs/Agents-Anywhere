# Reference-only runtime code

This directory contains pre-protocol Codex and Claude adapter implementations.
They are retained as migration reference material while native `AgentRuntime`
implementations are rewritten under `connector.runtimes`.

Active Connector code must not import modules from this directory.
