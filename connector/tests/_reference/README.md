# Reference-only tests

These files were moved out of active pytest collection together with the
pre-protocol Codex and Claude adapter implementations. They are retained as
migration examples and should be copied or rewritten when native runtime
packages are implemented.
